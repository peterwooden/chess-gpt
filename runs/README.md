# Run artifacts

Run outputs are generated and ignored by Git. Every run directory must be reproducible from a tracked experiment specification and must include a machine-readable manifest plus artifact hashes.
